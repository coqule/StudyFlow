// Módulo IA — construcción del contexto enviado a Gemini.
// Implementación real: feature 8 (ia_construir_contexto).
// Ver docs/reglas-ia.md ("Contexto enviado a Gemini") para el formato
// exacto que deberá devolver esta función.

function construirContexto(usuario_id) {
  throw new Error(
    "construirContexto no implementado (feature ia_construir_contexto)"
  );
}

module.exports = { construirContexto };
