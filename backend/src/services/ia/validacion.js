// Módulo IA — validación de la respuesta de Gemini antes de persistir.
// Implementación real: feature 10 (ia_validar_respuesta).
// Ver docs/reglas-ia.md ("Validación de respuesta") para el pseudocódigo
// de esquema, choques de horario y disponibilidad que deberá aplicar.

function validarRespuesta(respuestaRaw, disponibilidad) {
  throw new Error(
    "validarRespuesta no implementado (feature ia_validar_respuesta)"
  );
}

module.exports = { validarRespuesta };
